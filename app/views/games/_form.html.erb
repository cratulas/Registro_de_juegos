<%= form_with(model: game, local: true) do |form| %>
  <% if game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(game.errors.count, "error") %> prohibited this game from being saved:</h2>

      <ul>
      <% game.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :pdf, "subete un archivo dale" %>
    <%= form.file_field :pdf %>
  </div>

  <div class="field">
    <%= form.label :board_photo, "subete una foto dale" %>
    <%= form.file_field :board_photo %>
  </div>

 <div class="field">
    <%= form.label :pices_parts, "Partes y piezas" %>
    <%= form.file_field :pices_parts, multiple: true %>
  </div>







  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
